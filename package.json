{
  "name": "bm_keyvalidator",
  "version": "1.0.1",
  "description": "Business Manager extension for verifying that an SFCC private key (KeyRef) and an encoded public key form a matching keypair.",
  "private": true,
  "scripts": {
    "check:docs-cover": "node scripts/check-docs-cover.mjs",
    "test": "npm run check:docs-cover && npm run test:unit && npm run test:scripts",
    "release:check": "npm test",
    "preversion": "bash scripts/preversion.sh",
    "postversion": "git push --atomic origin main v$npm_package_version",
    "test:unit": "node --test test/unit/*.test.js",
    "test:scripts": "bash test/scripts/deploy.test.sh",
    "capture:cover": "node tools/cover/capture.mjs"
  }
}
